#spring: # 테스트에서는 아래처럼 주석처리해도 spring 이 알아서 메모리 db를 띄워줌
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        format_sql: true

logging:
  level:
    sql: debug
    org.hibernate.type: trace